version: '3.7'
services:
  jupyterlab-nlp-ja:
    build: .
    ports:
      - 8888:8888
    volumes:
      - ./work:/root/user/work:cached
    working_dir: /root/user/work
    command: bash -c "sh /mecab-test.sh && sh /run.sh"
    restart: always